{
  "name": "sunset-clock",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "dev": "nodemon ./bin/www",
    "setup-build": "npm i && cd frontend && npm i && npm run build",
    "setup-initial": "npm i && cd frontend && npm i",
    "preview": "npm run setup-build && npm run start"
  },
  "dependencies": {
    "axios": "^1.8.2",
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "dotenv": "^16.4.5",
    "express": "~4.21.2",
    "http-errors": "~1.6.3",
    "morgan": "~1.9.1",
    "nodemon": "^3.1.0",
    "pug": "3.0.3"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
